# 11 视觉伺服与抓取

> ⏳ 等 D405（明天到）。⭐ 官方已有完整实现可对照。

---

## 【速查】

| | |
|---|---|
| **两种伺服** | PBVS（位置型，误差在 3D）／ IBVS（图像型，误差在像素）|
| **两种安装** | ⭐ 眼在手外（不改动力学）／ 眼在手上（精度高但会盲抓）|
| ⭐ **建议** | 阶段 3 先试**眼在手外** |
| ⚠️ **夹爪** | 力反馈**不能换算指尖力** ⇒ 奖励要用"提起高度" |
| ⭐ **渲染 gap 对策** | **解耦**：位姿估计不进 RL |

---

## 一、前置知识

### 1.1 相机是怎么"看见"位置的

相机把 3D 世界压成 2D 图像 —— ⚠️ **深度信息丢了**。

一张普通照片里，你分不清"小而近"还是"大而远"。

⭐ **三种找回深度的办法**：

| 方法 | 原理 | 例子 |
|---|---|---|
| **双目** | 两个镜头视差（和人眼一样）| ⭐ **D405 就是这种** |
| 结构光 | 打出已知图案，看变形 | Kinect v1 |
| ToF | 测光飞回来的时间 | 激光雷达 |

### 1.2 什么是"内参"和"外参"

| | 描述什么 | 什么时候变 |
|---|---|---|
| **内参** | 相机**自己**的性质：焦距、光心、畸变 | ⭐ 出厂就定了 |
| **外参** | 相机**在哪**：相对世界或机器人的位姿 | ⚠️ 一动就变 |

⭐ **手眼标定求的就是外参** —— 相机相对机器人的固定变换。

### 1.3 什么是"手眼标定"

⭐ **求出"相机"和"机器人"之间的固定变换。**

比喻：你戴着 VR 头显，头显里看到一个杯子在"前方 30 cm"。
但你的**手**要伸到哪里？
⇒ 需要知道"**头显**相对"**手**"的位置关系。

数学上解 $AX = XB$（眼在手上）：

* $A$：机器人从姿态 1 到姿态 2 的运动（**从编码器算**）
* $B$：相机看到的标定板从姿态 1 到姿态 2 的运动（**从图像算**）
* $X$：⭐ **要求的**相机到末端的变换

⚠️ 要摆**很多个姿态**才能解出来（官方推荐 $8\times 8$ 棋盘格）。

### 1.4 什么是"抓取位姿"

不只是"物体在哪"，还要"**夹爪该怎么伸过去**"：

| 要素 | 说明 |
|---|---|
| **位置** | 夹爪中心放在哪 |
| **朝向** | ⚠️ 从上往下抓？从侧面抓？ |
| **开度** | 张开多大 |
| ⭐ **接近方向** | 从哪个方向靠近（避免碰倒物体）|

⚠️ 同一个物体常常有**多个**可行抓取位姿，要挑最稳的。
⇒ 这就是 GraspNet 这类算法在做的事。

---

## 二、先说人话

前面所有控制都假设"**我知道目标在哪**"。

现实是：目标在桌上，位置每次都不一样。**得先看见它。**

⭐ 两条路：

| | 做法 | 比喻 |
|---|---|---|
| **看一眼再动**（open-loop）| 拍照 → 算出物体位姿 → 规划 → 执行 | 闭着眼睛走路 |
| ⭐ **边看边动**（视觉伺服）| 每一拍都看，用**图像误差**直接驱动 | 睁着眼睛走 |

⚠️ 第一种对标定误差**极其敏感**——标定差 5 mm，抓取就偏 5 mm。
第二种能**自动补偿**，因为它一直在看。

---

## 三、两种视觉伺服

| | **PBVS**（位置型） | **IBVS**（图像型） |
|---|---|---|
| 误差定义在 | ⭐ **笛卡尔空间**（3D 位姿）| ⭐ **图像平面**（像素）|
| 需要 | 物体 3D 位姿估计 | 只要特征点像素坐标 |
| 相机标定敏感度 | ⚠️ 高 | ⭐ 低 |
| 轨迹 | ⭐ 笛卡尔直线（好看）| ⚠️ 图像直线，3D 轨迹可能很怪 |
| 会不会丢失目标 | ⚠️ 可能出视野 | ⭐ 天然保持在视野内 |

⭐ `官方` `Panthera_HT_SDK_Extensions` 用的是 **PBVS**
（ArUco marker 位姿误差 + 速度雅可比）。

---

## 四、⭐⭐ 眼在手上 vs 眼在手外

我们`实测`核对官方仓库发现**两种方案并存**：

| | **眼在手外**（ROS2 URDF 定义）| **眼在手上**（SDK_Extensions）|
|---|---|---|
| 出处 | `camera_mount_link`，父级 `base_link`，位置 `[0.4,-0.25,0.35]` | D405 装末端 |
| 标定 | ⭐ 简单（相机不动）| ⚠️ 手眼标定 |
| ⭐ **末端负载** | ⭐ **不增加** | ⚠️ +100 g |
| 视野 | ⭐ 全局，不被手臂挡 | 跟着手动 |
| 精度 | 远距离，分辨率低 | ⭐ 近距离，精度高 |
| ⚠️ 抓取末段 | ⭐ 看得见 | ⛔ **D405 最近 7 cm，必然盲抓** |

### ⭐ 建议：先试眼在手外

三个理由，第一个最重要：

1. ⭐⭐ **不改变动力学** ⇒ 辨识结果继续有效
2. 标定简单，误差不会和零位漂移叠加
3. 抓取末段还看得见

### ⚠️ 如果非要眼在手上：装在 J6 转轴上

`实测`（100 g 相机，3000 个随机构型取峰值）：

| 安装位置 | J6 重力峰值 |
|---|---|
| ⭐ **转轴上**（y=0）| **0.0000 N·m** ← 结构特性保留 |
| 侧偏 4 cm | 0.0392 N·m |

⭐ link6 质心恰好落在 J6 转轴上，所以 J6 的重力力矩**结构性地为零**
（见 [02 重力补偿](02_重力补偿.md)）。装偏了这个特性就没了 ——
J6 从"重力永远不做功"变成"静止时也要一直出力"。

另外末端加 100 g ⇒ `实测` J5 重力峰值 0.446 → **0.659 N·m（+48%）**。

---

## 五、手眼标定

**要求什么**：相机坐标系和末端（或底座）坐标系之间的固定变换 $T$。

**怎么求**：让手臂摆多个姿态，每个姿态记录

* 机器人的 TCP 位姿（从编码器 + FK）
* 相机看到的标定板位姿

然后解 $AX = XB$（眼在手上）或 $AX = ZB$（眼在手外）。

`官方` 脚本：`9_hand_eye_calibration.py`，$8\times 8$ 棋盘格（内角点 $7\times 7$、方格 20 mm），
**重力补偿下手动拖动**采样。

### ⚠️ 我们的 TCP 定义必须和 SDK 一致

⭐ 这就是为什么我们把 TCP 从 0.1893 改成 **0.165**（官方 `tool_link`）——
标定用的 TCP 位姿如果和 SDK 算的不一样，标定结果就是错的。

`实测` 那 24.3 mm 的差异会**直接变成抓取位置误差**。

---

## 六、⚠️ 夹爪：力反馈不可靠

`官方` 客服 Q18 回答：

> 「可用 `tqe` 做**近似**接触/保持控制，但**没有恒力闭环保证**」
> 「**不能直接换算**；指尖力换算系数**未提供**」

⇒ 三个后果：

| 想做的 | 能不能 |
|---|---|
| 恒力夹持 | ⚠️ 只能近似 |
| 测出"我夹了多少牛" | ❌ 不能 |
| ⭐ **判断"接触上了"** | 🟡 能（力矩突变）|

### ⚠️⚠️ 这直接影响 RL 的奖励设计

如果奖励只看"夹爪闭合到某个开度"，策略会学出**夹空气也拿奖励**。

⭐ **对策**：奖励里必须有「**提起高度**」项 ——
只有真的把物体拿起来了，高度才会变。这个判据不依赖力反馈。

---

## 七、官方 GraspNet 方案 = 我们的基线

`Panthera-HT_graspnet`：YOLO 检测 → PBVS 跟踪 → GraspNet 抓取规划 → 夹爪执行。

⭐ **这是我们的对照基线。** 要说明"RL + 视觉"有价值，就得和它比。

⚠️ 但客服没给成功率（Q24 未答）⇒ **我们得自己测一遍它**。

---

## 八、阶段 3 的新 gap：渲染

| 阶段 | gap 来源 |
|---|---|
| 1 轨迹跟踪 | 动力学 |
| 2 状态型抓取 | ＋接触 |
| ⭐ **3 视觉抓取** | ＋ **渲染** |

⭐ **对策：解耦。** 位姿估计做成独立模块，**不进 RL**。

| 方案 | obs | 训练成本 | 渲染 gap |
|---|---|---|---|
| 端到端（图像→CNN→动作）| 原始图像 | ⚠️ 慢一个数量级 | ⚠️ 巨大且难处理 |
| ⭐ **解耦** | 物体位姿（几维）| ⭐ **几乎不变** | ⭐ **转成位姿噪声** |

⭐ 解耦最大的好处：**把渲染 gap 变成位姿噪声**，
而位姿噪声能用域随机化直接处理，渲染 gap 不行。

⚠️ 前提：**阶段 2 训练时就要给物体位姿注入噪声**，
幅度取视觉模块的实测误差。做到了，阶段 3 就不用重训。

---

## 九、记住这一句

> **先试眼在手外——不改变动力学，辨识结果继续有效。
> 视觉走解耦方案：位姿估计不进 RL，把渲染 gap 转成位姿噪声。
> ⚠️ 夹爪力反馈不能换算指尖力，所以奖励必须有"提起高度"项，
> 否则会学出夹空气。**
